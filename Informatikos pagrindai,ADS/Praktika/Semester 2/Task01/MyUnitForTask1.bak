unit unitForTask1;

interface
type Array_Of_Numbers = array[1..1000] of integer;
     Array_Of_String = array [1..10000] of string;

implementation

function Modify(str:string;):string;
var lengthOfStr:integer;
begin
    lengthOfStr:=length(str);
    c2:=str[1];
    for p:=2 to lengthOfStr do begin
        c1:=str[p];
        if (c2==c1) then ammount:=ammount+1
        else if ammount=1 then Modify:=Modify+c2
        else Modify:=Modify+c2+'$'+ammout;
        c2:=c1;

    end;
end;

END.//End of Unit
